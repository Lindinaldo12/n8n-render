services:
  - type: web
    name: n8n
    env: docker
    plan: free
    region: ohio
    envVars:
      - key: WEBHOOK_URL
        fromService:
          type: web
          name: n8n
          property: hostport
